# 文件类型分类配置指南

## 概述
本系统的核心功能之一是**按类型智能统计文件**。所有文件类型分类逻辑在 `main.py` 的 `file_type_categories` 字典中定义。

## 默认支持的13种文件类型
系统默认识别并分类以下类型：
1.  **压缩包** (.zip, .rar...)
2.  **Word文档** (.doc, .docx...)
3.  **Excel文档** (.xls, .xlsx, .csv...)
4.  **PPT文档** (.ppt, .pptx)
5.  **PDF文档** (.pdf)
6.  **图片** (.jpg, .png, .gif...)
7.  **视频** (.mp4, .avi...)
8.  **音频** (.mp3, .wav...)
9.  **文本** (.txt, .md, .log...)
10. **代码** (.py, .java, .js, .html...)
11. **可执行文件** (.exe, .dmg, .sh...)
12. **配置文件** (.ini, .conf...)
13. **其他** (未匹配以上任一类别的文件)

## 如何自定义或添加新的文件类型？
如需新增一类文件（如“设计稿”），只需两步：
1. 在 `file_type_categories` 字典中添加一个新键值对。
2. 在新键对应的列表中添加相关的文件扩展名（带点号）。

### 示例：添加“设计稿”类型
```python
file_type_categories = {
    ... # 原有的其他类型
    '设计稿': ['.psd', '.ai', '.sketch', '.fig', '.xd'],
    '其他': []
}
修改后，所有 .psd, .ai 等文件将被统计在“设计稿”类别下。


# 极简用户手册

## 一句话说明
这是一个“一键统计文件夹里有多少文件，并自动生成漂亮Excel报表”的工具。

## 使用前准备
1.  确保电脑已安装Python（如果运行报错，可能是环境没有配置好，请参考requirements.txt安装所需库）。
2.  将本工具整个文件夹放在**你需要统计的所有文件夹的“外面”**。
    - ✅ **正确**：工具文件夹和 `张三的作业`、`李四的报告` 等文件夹在同一层。
    - ❌ **错误**：工具文件夹放在某个人的文件夹里面。

## 操作步骤
1.  **双击运行** `main.py` 文件。
2.  等待黑色窗口弹出并自动运行，过程中会显示正在处理的文件夹名。
3.  运行结束后，窗口会显示“统计完成！”，并告诉你生成的Excel文件名。
4.  **按回车键**关闭窗口。
5.  在当前文件夹里，找到一个名字类似 `文件夹统计报告_20260117_143022.xlsx` 的新Excel文件，打开即可查看统计结果。

## 报告怎么看？
生成的Excel报告包含3个标签页：
1.  **文件夹统计**：一个大的表格，横向是每个人/文件夹，纵向是各种文件类型和子文件夹，交叉点的数字就是文件数量。最后有“总计”行。
2.  **文件清单**：详细列出了**每一个文件**具体在哪个主文件夹、哪个子文件夹（或类型）下。用于具体追溯。
3.  **汇总分析**：告诉你哪类文件最多、平均每个文件夹有多少文件等深度信息。

## 常见问题
- **Q: 为什么运行后什么都没发生？**
  A: 请确认你放置工具的目录位置是否正确（参考“使用前准备”第2点）。
- **Q: 生成的Excel文件样式错乱？**
  A: 请确保电脑上安装了Microsoft Excel或兼容的办公软件（如WPS）。